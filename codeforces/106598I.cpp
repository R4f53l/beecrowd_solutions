#include <bits/stdc++.h> 

using namespace std; 

#define _ ios_base::sync_with_stdio(0);cin.tie(0); 

#define MAX 11

vector<bool> num_val(MAX); 

void backtrack(string s, int i){
    if(s.size() == 11)
        cout << s << endl;
    
}

int main(){_ 
    string s; cin >> s; 
    if(s.size() > 11){
        cout << "0" << endl; 
        return 0; 
    }
    int start;
    for(int i = 0; i < s.size(); i++){

    }
    
    
    

}